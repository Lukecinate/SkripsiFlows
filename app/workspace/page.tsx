"use client";
import { ChangeEvent, DragEvent, useEffect, useMemo, useState } from "react";
import { ingestSource } from "../../lib/ingestion";
import { analyzeCitations, formatReference, parseReferences } from "../../lib/citation";
import { exportDocx } from "../../lib/export-docx";
import { validateDocument } from "../../lib/validation";
import type { DocumentBlock, SkripsiDocument } from "../../lib/document-model";
import { createSnapshot, parseSnapshot, pushHistory, redo, SESSION_KEY, serializeSnapshot, undo } from "../../lib/session";
const styles = ["APA 7", "IEEE", "Vancouver", "Harvard", "Chicago"];
export default function WorkspacePage() {
  const [document, setDocument] = useState<SkripsiDocument | null>(null); const [paste, setPaste] = useState(""); const [selectedStyle, setSelectedStyle] = useState("APA 7"); const [draggedId, setDraggedId] = useState<string | null>(null); const [message, setMessage] = useState("Belum ada bahan masuk"); const [history, setHistory] = useState({ past: [] as SkripsiDocument[], present: null as SkripsiDocument | null, future: [] as SkripsiDocument[] }); const [savedAt, setSavedAt] = useState<number | null>(null);
  useEffect(() => { const raw = window.localStorage.getItem(SESSION_KEY); if (!raw) return; const snapshot = parseSnapshot(raw); if (!snapshot) { window.localStorage.removeItem(SESSION_KEY); return; } setDocument(snapshot.document); setPaste(snapshot.paste); setSelectedStyle(snapshot.selectedStyle); setHistory({ past: [], present: snapshot.document, future: [] }); setSavedAt(snapshot.savedAt); setMessage("Sesi terakhir berhasil dipulihkan."); }, []);
  useEffect(() => { if (!document) return; try { const snapshot = createSnapshot(document, paste, selectedStyle); window.localStorage.setItem(SESSION_KEY, serializeSnapshot(snapshot)); setSavedAt(snapshot.savedAt); } catch { setMessage("Autosave dihentikan karena ukuran sesi terlalu besar."); } }, [document, paste, selectedStyle]);
  const reviewCount = useMemo(() => document?.blocks.filter((block) => block.needsReview).length ?? 0, [document]);
  const referenceBlock = document?.blocks.find((block) => block.type === "reference");
  const citationAnalysis = useMemo(() => document && referenceBlock ? analyzeCitations(document.blocks.map((block) => block.content).join("\\n"), parseReferences(referenceBlock.content)) : null, [document, referenceBlock]);
  const validation = useMemo(() => document ? validateDocument(document, citationAnalysis?.references ?? document.references) : null, [document, citationAnalysis]);
  const commitDocument = (next: SkripsiDocument) => { setHistory((current) => pushHistory(current, next)); setDocument(next); };
  const parse = (content: string, kind: "paste" | "markdown" | "plain-text", name?: string, extension?: string) => { const result = ingestSource({ content, kind, name, extension }); if (!result.document) { setMessage(result.issues.map((issue) => issue.message).join(" ")); return; } commitDocument({ ...result.document, citationStyle: selectedStyle.toLowerCase().replaceAll(" ", "-") }); setMessage(result.issues.length ? `${result.issues.length} hal perlu ditinjau` : "Struktur berhasil dikenali"); };
  const upload = (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file) return; const extension = `.${file.name.split(".").pop()?.toLowerCase()}`; if (![".md", ".markdown", ".txt"].includes(extension)) { setMessage("Gunakan file .md, .markdown, atau .txt"); return; } const reader = new FileReader(); reader.onload = () => parse(String(reader.result ?? ""), extension === ".txt" ? "plain-text" : "markdown", file.name, extension); reader.readAsText(file); };
  const drop = (event: DragEvent<HTMLLabelElement>) => { event.preventDefault(); const file = event.dataTransfer.files[0]; if (!file) return; const extension = `.${file.name.split(".").pop()?.toLowerCase()}`; if (![".md", ".markdown", ".txt"].includes(extension)) { setMessage("Gunakan file .md, .markdown, atau .txt"); return; } const reader = new FileReader(); reader.onload = () => parse(String(reader.result ?? ""), extension === ".txt" ? "plain-text" : "markdown", file.name, extension); reader.readAsText(file); };
  const reorder = (targetId: string) => { if (!document || !draggedId || draggedId === targetId) return; const blocks = [...document.blocks]; const from = blocks.findIndex((block) => block.id === draggedId); const to = blocks.findIndex((block) => block.id === targetId); const [moved] = blocks.splice(from, 1); blocks.splice(to, 0, moved); commitDocument({ ...document, blocks, updatedAt: new Date().toISOString() }); setDraggedId(null); };
  const downloadDocx = async () => { if (!document || !validation?.canExport) { setMessage("Selesaikan semua review dan peringatan sebelum export."); return; } const result = await exportDocx(document, `${document.title || "skripsiflow"}.docx`); const blob = new Blob([result.data], { type: result.mimeType }); const url = URL.createObjectURL(blob); const anchor = window.document.createElement("a"); anchor.href = url; anchor.download = result.filename; anchor.click(); URL.revokeObjectURL(url); setMessage("DOCX berhasil dibuat."); };
  const updateBlock = (block: DocumentBlock, content: string) => { if (!document) return; commitDocument({ ...document, blocks: document.blocks.map((item) => item.id === block.id ? { ...item, content, source: "manual", needsReview: false, confidence: 1, confidenceLevel: "high" } : item), updatedAt: new Date().toISOString() }); };
  return <main className="workspace-page"><header className="workspace-header shell"><a className="brand" href="/"><span className="brand-mark">S</span><span>Skripsi<span className="brand-accent">Flow</span></span></a><div className="workspace-title"><span className="eyebrow">WORKSPACE</span><strong>{document?.title ?? "Dokumen baru"}</strong></div><div className="session-actions"><span className="save-status">{savedAt ? "Autosave aktif" : "Belum disimpan"}</span><button className="secondary-button" onClick={() => { const next = undo(history); setHistory(next); setDocument(next.present); }}>Undo</button><button className="secondary-button" onClick={() => { const next = redo(history); setHistory(next); setDocument(next.present); }}>Redo</button><button className="secondary-button" onClick={() => { window.localStorage.removeItem(SESSION_KEY); setDocument(null); setHistory({ past: [], present: null, future: [] }); setSavedAt(null); }}>Mulai ulang</button></div></header><section className="workspace shell"><aside className="input-panel"><p className="eyebrow">LANGKAH 01 / 04</p><h1>Masukkan<br /><em>bahanmu.</em></h1><p className="panel-copy">Upload file atau tempel teks dari AI. Struktur akan dibaca otomatis.</p><label className="dropzone" onDragOver={(event) => event.preventDefault()} onDrop={drop}><span className="upload-icon">↑</span><strong>Tarik file ke sini</strong><span>Markdown atau TXT</span><input type="file" accept=".md,.markdown,.txt,text/plain,text/markdown" onChange={upload} /></label><div className="paste-divider"><span>atau tempel langsung</span></div><textarea value={paste} onChange={(event) => setPaste(event.target.value)} placeholder="Tempel hasil AI atau tulisanmu di sini..." /><button className="primary-button full-button" onClick={() => parse(paste, "paste")}>Analisis bahan <span>→</span></button><p className="privacy-note">✦ File diproses sementara dan tidak masuk ke log.</p></aside><section className="review-panel"><div className="review-toolbar"><div><p className="eyebrow">LANGKAH 02 / 04</p><h2>Review struktur</h2></div><div className="toolbar-right"><label>Gaya referensi<select value={selectedStyle} onChange={(event) => setSelectedStyle(event.target.value)}>{styles.map((style) => <option key={style}>{style}</option>)}</select></label><span className={`review-status ${document ? "ready" : ""}`}>{document ? `${validation?.reviewCount ?? reviewCount} temuan` : "Menunggu bahan"}</span></div></div>{!document ? <div className="empty-review"><span>✦</span><strong>Hasil analisismu akan muncul di sini</strong><p>Mulai dengan upload file atau paste bahan skripsi.</p></div> : <div className="block-list">{document.blocks.map((block) => <article className={`document-block ${block.needsReview ? "needs-review" : ""}`} key={block.id} draggable onDragStart={() => setDraggedId(block.id)} onDragOver={(event) => event.preventDefault()} onDrop={() => reorder(block.id)}><span className="drag-handle">⠿</span><div className="block-content"><span className="block-type">{block.type} · {Math.round(block.confidence * 100)}%</span><textarea value={block.content} onChange={(event) => updateBlock(block, event.target.value)} /><small>{block.needsReview ? "Periksa struktur atau isi blok ini" : "Struktur terdeteksi dengan baik"}</small></div></article>)}</div>}<div className="quality-card"><div><span className="eyebrow">QUALITY CHECK</span><strong>{validation?.score ?? 0}/100</strong></div><p>{validation?.canExport ? "Dokumen siap diekspor." : "Selesaikan item berwarna kuning sebelum export."}</p></div>{validation && !validation.canExport && <div className="review-modal" role="alert"><div><span className="eyebrow">MANUAL REVIEW</span><h3>Beberapa bagian perlu perhatianmu</h3><p>Periksa blok yang ditandai kuning. Edit isinya untuk menandai sebagai sudah diverifikasi.</p><ul>{validation.issues.slice(0, 5).map((issue, index) => <li key={`${issue.code}-${issue.blockId ?? index}`}>{issue.message}</li>)}</ul></div></div>}<div className="reference-preview">{citationAnalysis && <><div className="reference-heading"><strong>Referensi terdeteksi</strong><span>{citationAnalysis.issues.length} temuan</span></div>{citationAnalysis.references.map((reference, index) => <p key={reference.id}>{formatReference(reference, selectedStyle.toLowerCase().replaceAll(" ", "-") as "apa-7" | "ieee" | "vancouver" | "harvard" | "chicago", index)}</p>)}</>}</div><div className="review-footer"><span>{message}</span><button className="primary-button" onClick={downloadDocx} disabled={!document || reviewCount > 0}>Unduh DOCX <span>↓</span></button></div></section></section></main>;
}


