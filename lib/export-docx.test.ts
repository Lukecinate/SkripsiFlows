import test from "node:test"; import assert from "node:assert/strict"; import JSZip from "jszip"; import { exportDocx } from "./export-docx"; import type { SkripsiDocument } from "./document-model";
const document: SkripsiDocument = { schemaVersion: 1, id: "test", title: "Demo", templateId: "indonesia-standard-v1", citationStyle: "apa-7", blocks: [{ id: "b1", type: "chapter", content: "BAB I", confidence: 1, confidenceLevel: "high", source: "manual", needsReview: false, metadata: { inline: JSON.stringify([{ text: "BAB I ", marks: [] }, { text: "PENDAHULUAN", marks: ["bold"] }]) } }], references: [], reviewRequired: false, createdAt: "", updatedAt: "" };
test("exports OOXML inline marks and required parts", async () => { const result = await exportDocx(document); const zip = await JSZip.loadAsync(result.data); const xml = await zip.file("word/document.xml")?.async("string"); const contentTypes = await zip.file("[Content_Types].xml")?.async("string"); assert.ok(xml?.includes("<w:b/>")); assert.ok(contentTypes?.includes("word/document.xml")); assert.ok(zip.file("_rels/.rels")); });
